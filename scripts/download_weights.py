# Script utilitario para descargar los pesos preentrenados (.pth) desde
# almacenamiento externo (S3/GCS/HuggingFace Hub) hacia ml/weights/, ya que
# los binarios de modelo no se versionan en git (ver .gitignore).
