# ==============================================================================
# SpectraGuard — Deepfake / AI-Generated Image Detection System
# requirements.txt
# ==============================================================================

# --- Deep Learning & Visión ---------------------------------------------------
# ADVERTENCIA: torch/torchvision están en un build NIGHTLY, no una release
# estable. Es necesario porque, a julio 2026, ninguna release estable de
# PyTorch soporta oficialmente GPUs Blackwell (RTX 50-series, sm_120) — solo
# los nightly con CUDA 12.8+. Si esta máquina no tiene una RTX 50-series (o
# cuando PyTorch estabilice el soporte), usar la versión estable en su lugar:
#   pip install torch==2.2.2 torchvision==0.17.2
# Deliberadamente SIN fijar la fecha exacta del nightly (ni siquiera el mismo
# comando resuelve igual entre plataformas: torch y torchvision se publican
# como snapshots diarios independientes, y Windows/Linux no siempre quedan
# en el mismo día — fijar fechas exactas rompe el build cruzado, como pasó
# acá). Dejar que pip resuelva el par mutuamente compatible más reciente
# en cada plataforma es más confiable que perseguir una fecha fija.
--extra-index-url https://download.pytorch.org/whl/nightly/cu128
--pre
torch
torchvision

timm==1.0.7                 # backbones preentrenados (EfficientNet, etc.)
opencv-python-headless==4.10.0.84   # headless: sin dependencias de GUI en servidor
# insightface declara "opencv-python" (con GUI) como dependencia. Cada vez
# que se reinstale este archivo completo, pip puede traer esa variante
# encima de la headless de acá arriba (el Dockerfile ya lo corrige solo).
# Si pasa en el venv local, forzar de nuevo con:
#   pip uninstall -y opencv-python
#   pip install --force-reinstall --no-deps opencv-python-headless==4.10.0.84
insightface==1.0.1          # RetinaFace para detección/alineación de rostros (ONNX, no depende de torch)
onnxruntime-gpu==1.27.0     # backend de insightface; 1.28.0 no publica wheel Linux (rompe el build de Docker)
numpy==2.2.6                 # insightface exige numpy>=2
scipy==1.13.1
Pillow==10.2.0

# --- Backend & API -------------------------------------------------------------
Django==5.0.7
djangorestframework==3.15.2
django-cors-headers==4.4.0
drf-spectacular==0.27.2     # documentación OpenAPI/Swagger del endpoint
django-environ==0.11.2      # gestión de variables de entorno (.env)
gunicorn==22.0.0            # servidor WSGI de producción

# --- Base de Datos ---------------------------------------------------------------
psycopg2-binary==2.9.9

# --- Utilidades ------------------------------------------------------------------
python-dotenv==1.0.1
tqdm==4.66.4
PyYAML==6.0.3                # lee ml/training/config.yaml

# --- Testing & Calidad de código --------------------------------------------------
pytest==8.2.2
pytest-django==4.8.0
factory-boy==3.3.1
black==24.4.2
flake8==7.1.0
mypy==1.10.1
django-stubs==5.0.2
